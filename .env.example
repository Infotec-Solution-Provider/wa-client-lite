# ===========================================
# WA-CLIENT-LITE - Environment Configuration
# ===========================================

# --- API URLs ---
# URL para requisições ao servidor principal (substitua :clientName pelo nome do cliente)
REQUEST_URL=https://contec.inf.br/api/:clientName/wwebjs

# --- Arquivos e Diretórios ---
# Diretório onde os arquivos de mídia serão salvos
FILES_DIRECTORY="C:\wa-files"
# Diretório onde os logs de erro serão salvos
ERRORS_DIRECTORY="C:\wa-files\err"

# --- Banco de Dados Principal (MySQL) ---
DATABASE_HOST=127.0.0.1
DATABASE_USER=root
DATABASE_PASSWORD=9231
DATABASE_DATABASE=whatsapp-client

# --- Configurações do Chrome (para whatsapp-web.js) ---
# Caminho para o executável do Chrome (opcional)
CHROME_BIN="C:\Program Files\Google\Chrome\Application\chrome.exe"
# WebSocket endpoint do Chrome (opcional, para usar com chrome remoto)
CHROME_WS=

# --- Configurações do Baileys (mysql-baileys auth) ---
# Configurações do banco para armazenar sessões do Baileys
# Se não definido, usa as mesmas credenciais do connectionParams da instância
BAILEYS_AUTH_DB_HOST=localhost
BAILEYS_AUTH_DB_PORT=3306
BAILEYS_AUTH_DB_USER=root
BAILEYS_AUTH_DB_PASS=
BAILEYS_AUTH_DB_NAME=baileys_auth
BAILEYS_AUTH_TABLE_NAME=auth

# --- Agendamentos (Cron) ---
# Expressão cron para carregar avatares (padrão: a cada 4 horas)
CRON_LOAD_AVATARS="0 */4 * * *"
# Expressão cron para sincronizar mensagens (padrão: a cada 2 minutos)
CRON_SYNC_MESSAGES="*/2 * * * *"

# --- Notificações de Erro ---
# Números que receberão notificações de erro (separados por vírgula)
NOTIFY_NUMBERS=555131346499
# Se deve notificar fora do horário comercial (true/false)
NOTIFY_OUTSIDE_BUSINESS_TIME=false
# Horário de início do expediente (formato HH:MM)
BUSINESS_TIME_START=7:30
# Horário de fim do expediente (formato HH:MM)
BUSINESS_TIME_END=18:00

# --- Outras Configurações ---
# Se true, usa a data/hora local ao invés do timestamp da mensagem do WhatsApp
USE_LOCAL_DATE=false